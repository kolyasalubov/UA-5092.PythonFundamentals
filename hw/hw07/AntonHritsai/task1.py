def compare_numbers(a: int, b: int) -> int:
    """Return the greater of two integers.

    Args:
        a: The first integer.
        b: The second integer.

    Returns:
        The greater integer, or their common value if they are equal.
    """
    return max(a, b)